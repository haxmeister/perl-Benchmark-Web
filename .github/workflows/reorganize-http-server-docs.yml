name: Reorganize HTTP server docs

on:
  push:
    branches:
      - docs/fix-libh2o-install

permissions:
  contents: write

jobs:
  reorganize:
    if: github.event.head_commit.message == 'Stage HTTP server documentation layout'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/fix-libh2o-install

      - name: Reorganize adapters and documentation
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path
          import subprocess

          root = Path('benchmarks/http')
          moves = {
              'servers/linuxevent-http.pl': 'servers/linuxevent/linuxevent-http.pl',
              'servers/hyperman-http.pl': 'servers/hyperman/hyperman-http.pl',
              'servers/feersum-http.pl': 'servers/feersum/feersum-http.pl',
              'servers/mojo-http.pl': 'servers/mojo/mojo-http.pl',
              'servers/twiggy-http.pl': 'servers/twiggy/twiggy-http.pl',
              'servers/node-http.js': 'servers/node/node-http.js',
              'servers/go-http.go': 'servers/go/go-http.go',
              'servers/aiohttp-http.py': 'servers/aiohttp/aiohttp-http.py',
              'servers/libh2o-http.c': 'servers/h2o/libh2o-http.c',
          }
          for src, dst in moves.items():
              (root / dst).parent.mkdir(parents=True, exist_ok=True)
              subprocess.run(['git', 'mv', str(root / src), str(root / dst)], check=True)

          run = (root / 'run.pl').read_text()
          replacements = {
              'servers/linuxevent-http.pl': 'servers/linuxevent/linuxevent-http.pl',
              'servers/hyperman-http.pl': 'servers/hyperman/hyperman-http.pl',
              'servers/feersum-http.pl': 'servers/feersum/feersum-http.pl',
              'servers/mojo-http.pl': 'servers/mojo/mojo-http.pl',
              'servers/twiggy-http.pl': 'servers/twiggy/twiggy-http.pl',
              'servers/node-http.js': 'servers/node/node-http.js',
              'servers/go-http.go': 'servers/go/go-http.go',
              'servers/aiohttp-http.py': 'servers/aiohttp/aiohttp-http.py',
              'servers/libh2o-http.c': 'servers/h2o/libh2o-http.c',
          }
          for old, new in replacements.items():
              run = run.replace(old, new)
          (root / 'run.pl').write_text(run)

          ci = Path('.github/workflows/ci.yml')
          ci_text = ci.read_text()
          for old, new in replacements.items():
              ci_text = ci_text.replace(f'benchmarks/http/{old}', f'benchmarks/http/{new}')
          ci.write_text(ci_text)

          main_readme = r'''# HTTP server comparison

This directory contains the standalone HTTP/1.1 cross-server benchmark for Benchmark::Web.

No server is a dependency of Benchmark::Web. The runner detects the targets available on the machine, skips missing targets by default, and only requires every requested target when `--strict` is used.

## Quick start

From `benchmarks/http/`:

```sh
perl run.pl --smoke
```

Run a selected comparison:

```sh
perl run.pl \
  --servers=hyperman,feersum,mojo,node,go,aiohttp \
  --requests=50000 \
  --warmup=5000 \
  --connections=100 \
  --repeats=5
```

Require every selected target to be available:

```sh
perl run.pl --servers=hyperman,feersum,node --smoke --strict
```

Write a machine-readable report:

```sh
perl run.pl \
  --servers=hyperman,feersum,mojo,node \
  --requests=100000 \
  --repeats=7 \
  --json=results/http-comparison.json
```

## Server runners

Each target lives in its own directory. Its README owns installation instructions, setup details, adapter-specific settings, and a one-target smoke-test command.

| key | server | selection | setup and adapter notes |
| --- | --- | --- | --- |
| `linuxevent` | Linux::Event::Net::HTTP | default | [servers/linuxevent/README.md](servers/linuxevent/README.md) |
| `hyperman` | Hyperman | default | [servers/hyperman/README.md](servers/hyperman/README.md) |
| `feersum` | Feersum | default | [servers/feersum/README.md](servers/feersum/README.md) |
| `mojo` | Mojolicious | default | [servers/mojo/README.md](servers/mojo/README.md) |
| `node` | Node.js built-in `http` | default | [servers/node/README.md](servers/node/README.md) |
| `go` | Go `net/http` | default | [servers/go/README.md](servers/go/README.md) |
| `aiohttp` | Python aiohttp | default | [servers/aiohttp/README.md](servers/aiohttp/README.md) |
| `twiggy` | Twiggy/AnyEvent | explicit-only | [servers/twiggy/README.md](servers/twiggy/README.md) |
| `h2o` | libh2o evloop | explicit-only | [servers/h2o/README.md](servers/h2o/README.md) |

Default target set:

```text
linuxevent,hyperman,feersum,mojo,node,go,aiohttp
```

Twiggy is explicit-only because its current behavior does not complete this benchmark's long-lived keep-alive workload reliably. libh2o is explicit-only because it is a lower-level protocol/server reference rather than a peer application API.

## Workload contract

Every selected server is driven by the same raw client in `run.pl`.

Default request:

```text
GET /bench HTTP/1.1
Host: benchmark.test
```

Default response body: 32 bytes.

Every adapter returns HTTP 200 with a fixed `Content-Length` and an `application/octet-stream` body of exactly the requested size.

When `--request-body-bytes=N` is greater than zero, the client sends a POST with `Content-Length: N` and exactly N body bytes. Adapters consume the request body before returning the fixed response.

Each repeat:

1. starts a fresh server process;
2. opens the requested number of loopback TCP connections;
3. runs warmup requests without recording them;
4. runs the measured workload;
5. records throughput and client-visible latency;
6. stops the server;
7. rotates server order for the next repeat.

The primary comparison is a **single-process, single-application-execution-slot** benchmark. It is not a worker-scaling benchmark.

This is a protocol-stack comparison, not a claim that every framework performs identical application-layer work. Each adapter uses the smallest normal public API that receives the request and returns the same response shape. Server-specific choices are documented beside each adapter.

## Runner settings

### `--servers=LIST`

Comma-separated server keys to run.

```sh
perl run.pl --servers=hyperman,feersum,node
```

Default:

```text
linuxevent,hyperman,feersum,mojo,node,go,aiohttp
```

Unavailable targets are skipped unless `--strict` is used.

### `--requests=N`

Measured HTTP requests per server, per repeat.

Default: `20000`.

### `--warmup=N`

Unmeasured requests sent before each measured phase.

Default: `2000`.

Warmup exercises parser/runtime caches and establishes steady-state persistent connections.

### `--connections=N`

Number of simultaneous persistent TCP connections.

Default: `100`.

This changes concurrency, not total measured request count.

### `--pipeline=N`

Maximum outstanding HTTP/1.1 requests per connection.

Default: `1`.

`1` means no HTTP pipelining. Values above 1 intentionally benchmark pipelined HTTP/1.1.

Latency is measured from request write until the complete response is parsed, so queueing behind earlier pipelined requests is part of latency.

### `--request-body-bytes=N`

Fixed request body size.

Default: `0`.

`0` sends GET requests. Positive values send POST requests.

```sh
perl run.pl --servers=hyperman,feersum,mojo --request-body-bytes=4096
```

### `--response-bytes=N`

Fixed response body size returned by every adapter.

Default: `32`.

```sh
perl run.pl --response-bytes=65536
```

### `--repeats=N`

Complete measured runs per server.

Default: `5`.

Server order rotates between repeats. The summary reports medians rather than best-of values.

### `--timeout=SECONDS`

Maximum time allowed for server startup or a benchmark phase.

Default: `120`.

A timeout is a failed benchmark case, not a throughput result.

### `--strict`

Makes any requested unavailable server a fatal error. Use this for CI or published runs where silently omitting a target would invalidate the intended comparison.

`--no-strict` explicitly restores the default skip behavior.

### `--json=PATH`

Writes a machine-readable report containing the benchmark contract version, environment/runtime versions when detectable, complete configuration, skipped targets, every repeat, and median summary values.

Keep the JSON report with published results.

### `--smoke`

Runs a tiny correctness workload:

```text
requests:        500
warmup:          100
connections:       4
pipeline:           1
response bytes:     8
repeats:            1
timeout:            15 seconds
```

Use smoke mode to verify setup and keep-alive behavior. Do not publish smoke throughput as performance data.

### `--help`

Prints the option summary.

## More examples

Higher concurrency:

```sh
perl run.pl \
  --servers=hyperman,feersum,mojo,node,go,aiohttp \
  --requests=100000 \
  --warmup=10000 \
  --connections=500 \
  --repeats=7
```

HTTP/1.1 pipelining:

```sh
perl run.pl \
  --servers=hyperman,feersum,mojo \
  --requests=100000 \
  --connections=100 \
  --pipeline=16 \
  --repeats=7
```

Large responses:

```sh
perl run.pl \
  --servers=hyperman,feersum,mojo \
  --response-bytes=65536 \
  --requests=50000
```

Optional low-level reference:

```sh
perl run.pl \
  --servers=hyperman,linuxevent,h2o \
  --requests=50000 \
  --warmup=5000 \
  --connections=100 \
  --repeats=5
```

## Directory layout

```text
README.md
run.pl
servers/
    aiohttp/
    feersum/
    go/
    h2o/
    hyperman/
    linuxevent/
    mojo/
    node/
    twiggy/
```

Each server directory contains the adapter source and its README.

## Continuous integration

Repository CI uses small correctness workloads, including a required smoke comparison that does not select Linux::Event. Optional adapter CI may install benchmark targets temporarily to exercise their adapters; those installations are test fixtures, not Benchmark::Web dependencies.

CI throughput is not a publishable performance result.

## Interpreting results

The runner reports requests per second plus p50, p95, p99, and maximum client-visible latency. The final table uses the median value across repeats for each metric.

Loopback throughput is useful for comparing CPU/protocol-stack cost, but it is not internet request capacity. Real network latency and bandwidth are deliberately absent, and client plus server compete for resources on one machine.

For public results:

- use a quiet stable machine or dedicated runner;
- report CPU model, OS/kernel, and runtime/server versions;
- keep workload settings identical;
- use multiple repeats and report medians;
- do not compare one-worker with multi-worker results as if they were the same benchmark;
- avoid unrelated CPU/network work;
- retain the JSON report;
- distinguish directional shared-CI results from dedicated-machine measurements.

GitHub-hosted runner throughput is useful for regression direction and correctness, but absolute numbers can vary substantially between runner instances.

## Adapter contract

Contributions from server authors are welcome.

A primary-comparison adapter should:

1. listen on `127.0.0.1:$ENV{BENCH_PORT}`;
2. use `$ENV{BENCH_RESPONSE_BYTES}` for the fixed body size;
3. consume a request body before responding;
4. return HTTP 200 with a fixed `Content-Length`;
5. use one server process and one application execution slot;
6. disable access logging, compression, TLS, and unrelated middleware;
7. use the smallest normal public API of the server being measured.

If a server cannot satisfy one of those constraints, document the difference in that server's README rather than hiding it.
'''
          (root / 'README.md').write_text(main_readme)

          readmes = {
              'linuxevent': r'''# Linux::Event::Net::HTTP benchmark target

Runner key: `linuxevent`

Adapter: `linuxevent-http.pl`

This target benchmarks Linux::Event::Net::HTTP using one event loop and one server process.

## Install

For an installed release:

```sh
cpanm Linux::Event::Net::HTTP
```

Verify that the required modules load:

```sh
perl -MLinux::Event::Loop \
     -MLinux::Event::Net::HTTP::Connection \
     -MLinux::Event::Net::HTTP::Server \
     -e 'print "ok\n"'
```

Benchmark::Web does not depend on Linux::Event::Net::HTTP. It is only needed when this target is selected.

## Development checkout

You can benchmark an uninstalled source checkout without installing it system-wide:

```sh
cd /path/to/perl-Linux-Event-Net-HTTP
perl Makefile.PL
make

cd /path/to/perl-Benchmark-Web/benchmarks/http
BENCH_LINUXEVENT_ROOT=/path/to/perl-Linux-Event-Net-HTTP \
  perl run.pl --servers=linuxevent --smoke --strict
```

`BENCH_LINUXEVENT_ROOT` must point at a built checkout containing `blib/lib` and `blib/arch`.

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=linuxevent --smoke --strict
```

## Adapter modes

`BENCH_LINUXEVENT_MODE` selects the response path:

```text
natural       ordinary on_request -> Response->end
request-end   response completed from on_request_end
fast-final    on_request_final default-final path
```

Example:

```sh
BENCH_LINUXEVENT_MODE=fast-final \
  perl run.pl --servers=linuxevent --smoke --strict
```

For requests with bodies, `natural` defers the response until `on_request_end` so the body is consumed before responding.

`BENCH_READ_BUDGET_BYTES` is passed through as the connection class `read_budget_bytes` stream option. It defaults to `0`.

Do not mix Linux::Event modes in one published result without labeling them separately.
''',
              'hyperman': r'''# Hyperman benchmark target

Runner key: `hyperman`

Adapter: `hyperman-http.pl`

## Install

Install Hyperman with your normal CPAN client:

```sh
cpanm Hyperman
```

Verify it:

```sh
perl -MHyperman -e 'print "$Hyperman::VERSION\n"'
```

Benchmark::Web does not depend on Hyperman. It is only needed when this target is selected.

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=hyperman --smoke --strict
```

A normal measurement example:

```sh
perl run.pl \
  --servers=hyperman \
  --requests=50000 \
  --warmup=5000 \
  --connections=100 \
  --repeats=5
```

## Adapter setup

The adapter uses Hyperman's PSGI-style application API. It configures `workers => 1` so the benchmark stays in one process/application execution slot and explicitly disables compression. For very large benchmark request bodies it raises Hyperman's body limit enough to accept the configured workload.
''',
              'feersum': r'''# Feersum benchmark target

Runner key: `feersum`

Adapter: `feersum-http.pl`

## Install

Install Feersum with your normal CPAN client:

```sh
cpanm Feersum
```

Verify the runner module:

```sh
perl -MFeersum::Runner -e 'print "ok\n"'
```

Benchmark::Web does not depend on Feersum. It is only needed when this target is selected.

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=feersum --smoke --strict
```

## Adapter setup

The adapter uses `Feersum::Runner` with prefork disabled, keep-alive enabled, unlimited requests per connection, and quiet mode enabled. Request bodies are consumed before the fixed benchmark response is sent.
''',
              'mojo': r'''# Mojolicious benchmark target

Runner key: `mojo`

Adapter: `mojo-http.pl`

## Install

Install Mojolicious with your normal CPAN client:

```sh
cpanm Mojolicious
```

Verify it:

```sh
perl -MMojolicious -e 'print "$Mojolicious::VERSION\n"'
```

Benchmark::Web does not depend on Mojolicious. It is only needed when this target is selected.

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=mojo --smoke --strict
```

## Adapter setup

The adapter uses a `Mojolicious` application with `Mojo::Server::Daemon`, one process, silent server output, a high request limit, and keep-alive enabled for the benchmark workload. The `/bench` route consumes the request body and returns the fixed response payload.
''',
              'twiggy': r'''# Twiggy benchmark target

Runner key: `twiggy`

Adapter: `twiggy-http.pl`

This target is explicit-only and is not part of the default comparison set.

## Install

Install Twiggy with your normal CPAN client:

```sh
cpanm Twiggy
```

Twiggy will install its normal AnyEvent dependencies.

Verify it:

```sh
perl -MTwiggy -e 'print "$Twiggy::VERSION\n"'
```

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=twiggy --smoke --strict
```

## Current benchmark status

Twiggy is explicit-only because current Twiggy behavior closes the long-lived keep-alive connections used by this workload before the requested phase completes reliably. Keep that limitation attached to Twiggy-specific results rather than treating an incomplete run as comparable throughput.

The adapter uses `Twiggy::Server` directly and consumes request bodies before returning the fixed PSGI response.
''',
              'node': r'''# Node.js benchmark target

Runner key: `node`

Adapter: `node-http.js`

This target uses Node.js's built-in `node:http` module. There are no npm dependencies.

## Install

Install a current Node.js runtime using your operating system or Node.js distribution method. On Debian/Devuan systems, the distribution package is typically:

```sh
sudo apt update
sudo apt install nodejs
```

Verify it:

```sh
node --version
```

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=node --smoke --strict
```

## Adapter setup

The runner launches one Node.js process. The adapter uses only `node:http`, consumes the incoming request stream, and sends the fixed benchmark response after the request body ends.
''',
              'go': r'''# Go net/http benchmark target

Runner key: `go`

Adapter: `go-http.go`

This target uses only Go's standard `net/http` package. There are no third-party Go modules.

## Install

Install the Go toolchain. On Debian/Devuan systems:

```sh
sudo apt update
sudo apt install golang-go
```

Verify it:

```sh
go version
```

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=go --smoke --strict
```

`run.pl` compiles `go-http.go` to a temporary binary before the benchmark and removes that binary afterward.

## Adapter setup

The adapter calls `runtime.GOMAXPROCS(1)` so the comparison remains one application execution slot. It uses the standard `net/http` server, drains request bodies, and returns the fixed response payload.
''',
              'aiohttp': r'''# Python aiohttp benchmark target

Runner key: `aiohttp`

Adapter: `aiohttp-http.py`

## Install

A virtual environment keeps the benchmark dependency out of the system Python installation. On Debian/Devuan systems:

```sh
sudo apt update
sudo apt install python3 python3-venv
```

From `benchmarks/http/` create the environment and install aiohttp:

```sh
python3 -m venv .venv
.venv/bin/pip install aiohttp
```

Verify it:

```sh
.venv/bin/python -c 'import aiohttp; print(aiohttp.__version__)'
```

The benchmark runner invokes `python3`, so put the virtual environment first in `PATH` when running this target:

```sh
PATH="$PWD/.venv/bin:$PATH" perl run.pl --servers=aiohttp --smoke --strict
```

Benchmark::Web does not depend on aiohttp. The virtual environment is only a local benchmark setup.

## Adapter setup

The adapter uses `aiohttp.web`, disables access logging, consumes request bodies, and returns the fixed benchmark response from `/bench`.
''',
              'h2o': r'''# libh2o evloop benchmark target

Runner key: `h2o`

Adapter: `libh2o-http.c`

This target is explicit-only. It measures libh2o's native event-loop HTTP server API as a lower-level reference implementation rather than a peer application framework.

## Required library

The runner requires a C compiler, `pkg-config`, and an installed `libh2o-evloop` that is visible through:

```sh
pkg-config --modversion libh2o-evloop
```

The `libh2o-evloop-dev` package is not available on every Debian-derived release, so do not assume that package name exists on a particular Debian, Ubuntu, or Devuan release.

## Build current upstream H2O

On Debian/Ubuntu/Devuan systems, install the build prerequisites:

```sh
sudo apt update
sudo apt install build-essential cmake pkg-config libssl-dev zlib1g-dev git
```

Clone H2O with its submodules and install it:

```sh
git clone --recurse-submodules https://github.com/h2o/h2o.git
cd h2o
mkdir -p build
cd build
cmake ..
make -j"$(nproc)"
sudo make install
sudo ldconfig
```

Verify the library required by Benchmark::Web:

```sh
pkg-config --modversion libh2o-evloop
```

If H2O was installed under a custom prefix and `pkg-config` cannot find it, add the prefix's `lib/pkgconfig` directory to `PKG_CONFIG_PATH`. For the common `/usr/local` prefix:

```sh
export PKG_CONFIG_PATH=/usr/local/lib/pkgconfig${PKG_CONFIG_PATH:+:$PKG_CONFIG_PATH}
pkg-config --modversion libh2o-evloop
```

If your distribution actually provides `libh2o-evloop-dev`, installing that package together with `build-essential` and `pkg-config` is a valid shortcut. Check first with:

```sh
apt-cache show libh2o-evloop-dev
```

## Run

From `benchmarks/http/`:

```sh
perl run.pl --servers=h2o --smoke --strict
```

The runner compiles `libh2o-http.c` to a temporary binary using the flags returned by `pkg-config --cflags --libs libh2o-evloop`.

## Compatibility note

A successful `pkg-config --modversion libh2o-evloop` proves that the library installation is visible to the benchmark. If the adapter's compile/link step then fails, that is a libh2o API/ABI compatibility problem in the adapter rather than an installation-detection failure. Current upstream compatibility is tracked separately from these installation instructions.
''',
          }

          for name, content in readmes.items():
              (root / 'servers' / name / 'README.md').write_text(content)
          PY

          perl -c benchmarks/http/run.pl
          node --check benchmarks/http/servers/node/node-http.js
          python3 -m py_compile benchmarks/http/servers/aiohttp/aiohttp-http.py
          go build -o /tmp/benchmark-web-go-layout-check benchmarks/http/servers/go/go-http.go

          git config user.name 'Benchmark::Web CI'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git commit -m 'Organize HTTP server adapters and setup docs'
          git push origin HEAD:docs/fix-libh2o-install
